apiVersion: 1

providers:
  - name: 'dnscluster'
    orgId: 1
    folder: 'DNS Cluster'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
